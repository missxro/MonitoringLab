<!DOCTYPE html>
<html lang="es">
<head><meta charset="utf-8"><title>{{ inventory_hostname }}</title></head>
<body>
  <h1>{{ inventory_hostname }}</h1>
  <p>Cliente: {{ lab.hosts[inventory_hostname].cliente }}</p>
  <p>Esta página existe únicamente para generar access.log/error.log de
     demostración para el laboratorio de monitorización. Cada petición
     queda registrada y Alloy la envía a Loki.</p>
</body>
</html>
